
@model IEnumerable<QLBH.Fastfood.Models.ChiTietDonHang>
@{
    ViewBag.Title = "Chi tiết đơn đặt hàng";
    Layout = "~/Views/Layout/AdminLayout.cshtml";
}

<ol class="breadcrumb">
    <li class="breadcrumb-item"><a href="@Url.Action("Index")">Trang chủ</a> <i class="fa fa-angle-right"></i></li>
    <li class="breadcrumb-item"><a href="@Url.Action("List")">Sản phẩm</a> <i class="fa fa-angle-right"></i></li>
</ol>
<div class="agile-grids">
    <div class="agile-tables">
        <div class="w3l-table-info">
            <hr />
            <div class="w3-search-box">
                <form action="~/ProductManage/List" method="post">
                    <input type="text" placeholder="Tìm kiếm..." name="keyword" id="keyword">
                    <input type="submit" value="">
                </form>
            </div><!--//end-search-box-->
            <p>@ViewBag.message</p>
            <div class="clearfix"> </div>
            <hr />
            <h2>Chi Tiết Đơn Đặt Hàng: @ViewBag.MaDH</h2>
            <table id="table">
                <thead>
                    <tr>
                        <th>
                            Mã DĐH
                        </th>
                        <th>
                            Mã SP
                        </th>
                        <th>
                            Số Lượng
                        </th>
                        <th>
                            Đơn Giá
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.MaDH)
                            </td>
                            <td style="color:brown">
                                <b>@Html.DisplayFor(modelItem => item.SanPham.TenSP)</b>
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.SoLuong)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.GiaTien)
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
            @if (ViewBag.IsApproved == false)
            {
                @Html.ActionLink("Duyệt", "OrderApproval", new { ID = ViewBag.MaDH }, new { @class = "btn btn-primary" });
            }
            <br />
        </div>
    </div>
</div>

